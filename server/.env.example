# Imposter Game Server - Environment Variables Template
# Copy this to `.env` and fill in your actual values
# DO NOT commit `.env` to version control

# Server Configuration
NODE_ENV=development
PORT=3000

# JWT Authentication
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-key-change-this-in-production-min-32-chars

# Database Configuration
# For development, leave commented to use SQLite (creates imposter_game.db)
# For production, set DATABASE_URL to PostgreSQL connection string

# PostgreSQL (Production/Render)
# Format: postgresql://username:password@hostname:5432/database_name
# DATABASE_URL=postgresql://user:password@localhost:5432/imposter_game
# USE_PG=true

# SQLite (Development - default if DATABASE_URL not set)
# Database will be stored in ./imposter_game.db

# Logging
LOG_LEVEL=info

# Frontend URL (for CORS if needed)
FRONTEND_URL=http://localhost:8000

# Optional: Rate Limiting Configuration
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX_REQUESTS=100
